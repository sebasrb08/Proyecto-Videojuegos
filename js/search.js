export function search(value,array){
    // console.log(value.value)
    // array.forEach(element => {
    //     const indice = element.nombre.indexOf(value.value);
    //         let filtrado=element.filter()
        
    // });
}